// Display 3–4 small image thumbnails.
// When the user clicks on one, show the clicked image in larger size above.
// (Hint: store selected image in a state variable)

import React from 'react'

const SimpleImgGallery=()=>{
    return(
        <div>
          
        </div>
    )
}
export default SimpleImgGallery